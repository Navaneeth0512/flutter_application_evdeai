import 'package:flutter/material.dart';

class ShareBusPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ShareBusPage'),
      ),
      body: Center(
        child: Text('This is the ShareBusPage'),
      ),
    );
  }
}

void main() {
  runApp(MaterialApp(
    home: ShareBusPage(),
  ));
}
